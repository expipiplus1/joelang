const string hello = "Hello",
             world = "World",
             hello_world = hello + ", " + world + "!";
const unsigned twenty = 20ul;

int a = 6l + 1;

const float twentyone = twenty + 1;

const int d_size = a;
int b[2],
    c[2] = {8, 9},
    d[d_size];

const string e = (a=0)+twentyone == 21 ? "foo" : "bar";

float4 my_vec;

technique t1
{
    pass p0
    {
        my_string_state = e;
        my_bool_state = a;
        my_bool_state = a &= 3;
        my_bool_state = a &= 0xf00;
    }

    pass p1
    {
        my_int_state = b[1];
        my_int_state = b[1] += 2;
        my_int_state = b[b[b[0]]+1];
        my_bool_state = hello_world == "Hello, World!";
        my_string_state = hello + ", " + world + "!";
        my_string_state = a == 0 ? hello : "bar";
    }

    pass p2
    {
        my_floating_state = 2 + twentyone == 23.0f;
        my_string_state = "Hello, World!";
        my_floating_state = one + 2;
        my_int_state = c[0];
        my_int_state = (b = c)[1];
    }
}


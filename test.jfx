const string hello = "Hello",
             world = "World",
             hello_world = hello + ", " + world + "!";
const unsigned twenty = float(20ul);

int a = 6l + 1;

const float twentyone = twenty + 1;

const int d_size = a;
int b[2],
    c[2] = {8, 9},
    d[d_size];

const string e = (a=0)+twentyone == 21 ? "foo" : "bar";

float f = {int(1)};

float4 my_vec = float4((a+1)/(a+1), 4, 3*(2.0+1), 0x10);

int t{0};
int q = 1;
int w = (t+=2)+=q;

const string strings[2] = {"foo", "bar"+"bar"};

pass p0;

int func(int);

void vunc();

int func2(bool b, float f[2])
{
}


technique t1
{
    p0;

    pass p1
    {
        my_int_state = b[1];
        my_int_state = b[1] += 2;
        my_int_state = b[b[b[0]]+1];
        my_bool_state = hello_world == "Hello, World!";
        my_string_state = hello + ", " + world + "!";
        my_string_state = a == 0 ? hello : "bar";
    }

    pass p2
    {
        my_floating_state = 2 + twentyone == 23.0f;
        my_string_state = "Hello, World!";
        my_floating_state = one + 2;
        my_int_state = c[0];
        my_int_state = (b = c)[1];
        //my_floating_state = my_vec[0];
        my_int_state = q;
        my_int_state = t;
        my_int_state = w;
        my_bool_state = 1 == int(1.1);
        my_float4_state = my_vec;
    }
}

pass p0
{
    my_string_state = e;
    my_bool_state = a;
    my_bool_state = a &= 3;
    my_bool_state = a &= 0xf00;
}
